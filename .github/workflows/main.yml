name: Metrics
on:
  schedule: [{cron: "0 0 * * *"}]
  workflow_dispatch:
jobs:
  github-metrics:
    runs-on: ubuntu-latest
    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          user: GenCloud-Ltd
          template: classic
          base: header # Запазваме само хедъра за заглавие
          
          # КРИТИЧНИ НАСТРОЙКИ ЗА PRIVATE REPOS
          config_allow_variant_repositories: yes
          repositories_batch: 50
          repositories_affiliations: owner, collaborator
          
          # ПЛЪГИН ЗА КОНТРИБЮТОРИ
          plugin_contributors: yes
          plugin_contributors_contributions: yes
          plugin_contributors_sections: contributors
          plugin_contributors_categories: commits
          plugin_contributors_limit: 15
          
          # ПЛЪГИН ЗА ХОРА (АВАТАРИ)
          plugin_people: yes
          plugin_people_types: contributors
          plugin_people_limit: 24
